Solve this problem by deliberately removing the most obvious approach. Force creative alternatives.

Rules:
1. Identify the DEFAULT solution everyone would reach for first
2. Explicitly BAN that approach: "You cannot use [default]"
3. Generate 3 alternative solutions that work WITHOUT the banned approach
4. For each alternative, explain: why it might actually be BETTER than the default
5. Pick the strongest alternative and develop it fully

The constraint is the creativity engine. The weirder the limitation, the more original the output.

If no specific constraint is given, auto-detect the "obvious" approach and ban it yourself.

$ARGUMENTS
