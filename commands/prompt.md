---
description: Alias for /prompt-master — generates optimized prompt for any AI tool.
---

Invoke the `prompt-master` skill with the user's task. See `/prompt-master` for full doc.

**Task:** $ARGUMENTS
