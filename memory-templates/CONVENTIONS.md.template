---
description: |
  Coding and workflow conventions specific to this user's projects.
  These override generic best practices where they conflict.
---

# Conventions

Project-specific rules that apply across all work. Not generic style guides —
things unique to this setup that Claude should respect without being reminded.

---

## Code Style

### TypeScript / JavaScript
- [FILL e.g., "No barrel index.ts files — import from the actual module path"]
- [FILL e.g., "Prefer named exports over default exports everywhere"]
- [FILL e.g., "No .js extension in imports — project uses bundler resolution"]

### CSS / Styling
- [FILL e.g., "Tailwind utility-first, no custom CSS classes unless animation requires it"]
- [FILL e.g., "No inline style objects unless driven by dynamic values"]

### File Naming
- [FILL e.g., "Components: PascalCase.tsx. Utilities: camelCase.ts. Config: kebab-case.ts"]

---

## Git & Branching

- [FILL e.g., "Branch naming: feat/short-slug, fix/short-slug, chore/short-slug"]
- [FILL e.g., "Commit message: imperative mood, 50 chars subject, no period"]
- [FILL e.g., "Never force-push to main — use revert commits"]

---

## Claude Code Workflow

- [FILL e.g., "Always run /cost before ending a session — log to LEARNINGS if over budget"]
- [FILL e.g., "Use subagent with model: 'haiku' for any grep/search task"]
- [FILL e.g., "Archive session with /archive after any session that changes more than 3 files"]

---

## Testing

- [FILL e.g., "Unit tests live next to source: foo.ts → foo.test.ts"]
- [FILL e.g., "No snapshot tests — they rot silently"]
- [FILL e.g., "Integration tests in /tests/integration — run with npm run test:int"]

---

*Template version: 1.0*
